import React from 'react';
/**
 * 使用方法：
 * <Pagination page={this.state.currentPage}
        size={10}
        len={3}
        total={51}
        btnSize='sm'
        onPageChange={this.onPageChange}
    />
 *
 * page: 当前页
 * size: 每页数据总数
 * total: 数据总量
 * btnSize: 不设置时为default，可设置为sm, lg, 影响高度
 * onPageChange: 点击翻页的回调函数
 */
declare const Temp: (props: any) => React.JSX.Element | null;
export default Temp;
